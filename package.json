{
  "name": "chichieh-huang.com",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "build:nosync": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-content.mjs",
    "migrate:medium": "node scripts/migrate-medium.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.7.0",
    "gray-matter": "^4.0.3"
  }
}
